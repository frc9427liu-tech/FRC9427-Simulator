# LEO 模擬搖桿

不用接機器人,在電腦上跑 LEO 的程式,用網頁上的兩隻搖桿操作,右邊看機器人怎麼動。
**完全不靠 Claude**,沒網路也能用。也**不會改到 LEO 專案**,上機的版本不受影響。

## 怎麼開

1. 雙擊桌面的 **「LEO 模擬搖桿」**(或這個資料夾裡的 `啟動模擬搖桿.bat`)
2. 等大約 10~30 秒,瀏覽器會自己打開搖桿網頁
3. 右上角按 **啟用**,就可以開始推搖桿

用完雙擊 **「LEO 結束模擬」**。

- 會用 Chrome(或 Edge)開成**獨立 App 視窗**,沒有網址列。要釘到工作列:在工作列的圖示按右鍵 →「釘選到工作列」
- 按了搖桿沒反應:看畫面下方的提示。通常是**還沒按「啟用」**,或是**開了兩個搖桿畫面**(一次只能一個控制)

## 操作

| | |
|---|---|
| 駕駛手 | 左搖桿上下 = 左輪、右搖桿上下 = 右輪(坦克式) |
| 操作手 十字鍵 ▲▼ | Intake 手臂收起/放下 |
| 操作手 LB / X | 吸球 / 吐球 |
| 操作手 左搖桿左右 | 砲台左右轉 |
| 操作手 RT | 飛輪轉起來 |
| 操作手 RT + A | 發射(飛輪沒轉就不送球) |
| 操作手 RB | 只轉 Orbit |

- 滑鼠一次只能按一個東西。要**同時按兩個**(例如 RT + A)→ 用**鍵盤**(下面),或打開上面的 **🔒 鎖定模式**:按一下就一直按著,再按一下才放開

### ⌨️ 鍵盤(可以同時按好幾個鍵)

| 駕駛手 | | 操作手 | |
|---|---|---|---|
| W / S | 前進 / 後退 | ↑ / ↓ | 手臂收起 / 放下 |
| A / D | 左轉 / 右轉 | ← / → | 砲台左右轉 |
| 按住 Shift | 慢速 | Q / E | 吸球 / 吐球 |
| | | R | 飛輪 開 / 關 |
| | | F | 發射(飛輪要先開) |
| | | G | 只轉 Orbit |

右邊搖桿上面有「⌨️ 鍵盤操作」可以展開看。有接實體手把的那一邊,鍵盤就不管它

### 🎬 展示模式

模擬畫面上方的紫色 **🎬 展示模式**:虛擬駕駛會自己 放下手臂 → 去吸球 → 收手臂 → 開到射擊點 → 對準 HUB → 發射,一直循環。
它是**真的在按搖桿**,訊號一樣送進模擬器給 LEO 程式算,不是播動畫 —— 展示給老師時可以一邊講程式、畫面一邊自己跑。
碰任何搖桿、按鍵盤、按空白鍵或停用,就會停下來把控制權還給你。
- **緊急停止**:鍵盤空白鍵或 Enter
- **自訂按鈕**:每個搖桿下面「＋ 新增按鈕」,取名字、選要按哪一顆,會記在這台電腦的瀏覽器裡。要刪掉就打開「✏️ 編輯按鈕」再點紅色 ✕
- 切到別的分頁或關掉網頁,機器人會自動停用

## 實體手把(Xbox)

1. USB 或藍牙接上電腦
2. 在搖桿網頁上**按一下手把的任一顆按鈕**(瀏覽器規定要先按一下才會讓網頁讀手把)
3. 第 1 支自動當**駕駛手**、第 2 支當**操作手**;每個控制器右上角的選單可以改
4. 用實體手把時,旁邊的網頁搖桿會亮綠框,而且會跟著你的手把動,可以用來確認有沒有接對

有實體手把就不需要鎖定模式,RT + A 直接一起按就好。

## 畫面

- 上面可以切 **3D / 2D 俯視**
- 鏡頭:**跟車**(像瑪利歐賽車)、**第一人稱**、**全場**、**轉播**
- **⛶ 全螢幕**:只放大模擬畫面,展示給教練看很好用(按 Esc 離開)

## 手機 / 平板

電腦開著模擬器的時候,手機連**同一個 Wi-Fi**,打開網頁最下面寫的網址(像 `http://10.x.x.x:8765`)。
在學校或沒有 Wi-Fi 的地方:筆電開 **行動熱點**(設定 → 網路和網際網路 → 行動熱點),手機連筆電的熱點就可以。
第一次 Windows 可能會跳「允許存取」,按允許。
模擬器一次只讓**一個**網頁控制,用手機時先把電腦上的那一頁關掉。

## ⚙️ 機構設定(跑別人的程式)

模擬器不只能跑 LEO。換成別的機器人專案時,按模擬畫面上方的 **⚙️ 機構設定**,告訴模擬器:

- 哪個數值是 **左輪 / 右輪出力**、**手臂位置**、**砲台角度**、**飛輪轉速**、**送球**、**Intake 滾輪**
- 數值可以選 **NetworkTables**(程式裡 `SmartDashboard.putNumber` 或 AdvantageKit 發的數字),或 **模擬硬體**(程式裡用了 SPARK MAX / Talon FX / PWM 馬達,模擬時會自己登記)
- 先按「啟用」動一動搖桿,右邊「目前數值」會跳的就是選對了
- 射球那一行填飛輪直徑、出球效率、仰角

按 💾 儲存,設定會存成那個專案資料夾裡的 `.robot-sim.json`(可以一起 commit,別人打開就有)。LEO 沒有這個檔也能用(內建預設)。

目前只支援**坦克式(左右輪)底盤**;swerve 還沒做。展示模式是照 LEO 的按鍵寫的,別的專案不會出現。

## 🤖 自訂機器人(換成自己隊的車)

按模擬畫面上方的 **🤖 自訂機器人**:

1. **車身 3D 模型**:選「我們自己的模型」→ 選 .glb 檔
   - Onshape:組合件分頁按右鍵 → **Export** → 格式選 **GLTF**(能選就勾 binary .glb)
   - 模型歪的、躺著的:用「繞 X / Y / Z 軸轉」調到車頭朝前;按 **👀 看一下** 可以先收起視窗看 3D 畫面,再按上方藍色條回來繼續調
   - 預設會自動縮到車身尺寸、自動加保險桿;模擬器自己的砲台 / 手臂 / 球預設藏起來(可以打開)
2. **保險桿**:紅 / 藍、隊號、隊徽圖片
3. **尺寸**:含保險桿的長和寬(碰撞、吸球口跟著變)
4. **底盤動力**:馬達型號、每邊幾顆、減速比、輪徑、整車重量、輪胎摩擦係數、電流限制 → 下面會算出理論極速、最大加速度、會不會打滑
5. **電池**:開路電壓、內阻(舊電池內阻大,電壓掉得多)

按 💾 儲存 → 存進專案資料夾的 `.robot-sim.json`(跟 ⚙️ 機構設定同一個檔)。模型檔和隊徽存在這台電腦,換電腦要重新選一次。

## 🌟 光線追蹤畫質

畫質選單選 **🌟 光線追蹤**:
- 車子、鏡頭、球在動的時候是一般畫面(流暢);**全部停下來 0.3 秒就自動變成路徑追蹤**,右下角會顯示取樣數,取樣越多畫面越乾淨、越像照片(真的柔和陰影、金屬 / 壓克力反射、光從地毯反彈到車底)
- 一動就馬上切回一般畫面
- 第一次切換要建立場地的加速結構,會停頓幾秒
- 需要好的獨立顯示卡(RTX 等級);手機、內顯請用「高」

## 🌍 物理(2026-09-25 v7:真的 3D 物理引擎)

- 換成 **Rapier 物理引擎**:車子是有質量、有轉動慣量的 3D 剛體,地上每顆球都是剛體
- 車撞到牆、HUB、TOWER、TRENCH 底座會真的被擋住、反彈;開上 BUMP 會被抬起來、翻過去
- 球會滾、會互相堆疊、被車撞會噴出去;手臂放下時 Intake 會把球擋在車頭推著走
- 輪胎有側向抓地力:被從側面推,力量要夠大才推得動(跟真的比賽一樣)
- 射出去的球在空中還是用原本的彈道(重力、空氣阻力、網子、進球判定),落地才交給物理引擎

## 🌍 物理(2026-09-24 v6)

- **底盤用真的馬達模型算**:電壓 → 馬達電流 → 推力;推力超過輪胎抓地力就打滑;所有馬達一起從電池抽電,電壓會掉(左上角 🔋 顯示電壓、⚡ 顯示底盤電流),低於 6.75 V 會 Brownout(跟真的 roboRIO 一樣把馬達關掉)
- 電流限制越低,起步越溫和、越不容易 Brownout;不限電流全力起步,電池很容易被拉垮
- 轉彎時輪胎會側滑,原地轉比直線吃力

## 🌍 物理(2026-09-23 v5)

- 車子有重量:起步有加速度、原地轉比較慢(輪胎側滑)、開上 BUMP 會變慢
- 車子是方形,撞到牆、HUB、TOWER 會停住(場地元件的位置和大小是從官方場地模型量的)
- 地上的球會滾、有摩擦力、會互相推擠,被車撞會噴出去
- 射出去的球有重力和空氣阻力,飛輪越快射越遠,**邊開邊射球會帶著車速**
- HUB 入口 1.83 m;射太遠會被 HUB 後面的網子擋下來掉進去;打到側面會彈開;進的球會從 HUB 後面滾回中場
- 程式裡標「示意」的數字(極速、加速度、球重、出球效率…)是還沒量到真實數值的合理值,量到了改 `physics.js` 最上面或 ⚙️ 機構設定就好

## 右邊的模擬畫面

- 機器人位置是用底盤出力**推算的示意動畫**;砲台角度、手臂長度也是示意比例(齒比還沒量)
- 但所有**數字都是模擬器裡 LEO 程式真的算出來的**
- 手臂或砲台轉超過行程會跳**紅色警告** —— 因為軟限位還沒打開,真的機器人這時候就會撞壞。這也是為什麼上機前一定要量好行程、打開軟限位
- 場上有球可以吸(手臂放下 + LB 開到球上),射進六角形目標會記分
- **落點預測圈**:飛輪一轉(RT)地上就會出現一個圈,是球會掉下去的地方。圈變**綠色**(3D 會跳到 HUB 上面)= 射得進,按 A;**橘色** = 射不進,左上角會寫要往前開還往後退幾公尺。LEO 的射擊轉速(80 圈/秒)射程約 3.9 公尺;正前方那個距離剛好是 TOWER,所以要從 HUB 的**斜前方**射(展示模式就是站那裡)。開車中射球,預測圈會算進車速
- HUB 是實心的,車子撞到會被擋住;球被推到牆邊也不會掉出場外

## 檔案說明(不用動)

| 檔案 | 做什麼 |
|---|---|
| `index.html` / `screen.js` | 搖桿網頁、模擬畫面 |
| `view3d.js` | 3D 畫面(官方場地模型、特效) |
| `demo.js` | 展示模式(虛擬駕駛) |
| `keyboard.js` | 鍵盤操作 |
| `server.js` | 把網頁給瀏覽器和手機的小伺服器(node) |
| `sim-ws.gradle` | 讓模擬器接受網頁搖桿,並關掉 WPILib 內建的模擬視窗(它會把搖桿歸零) |
| `sim-run.bat` / `start.ps1` / `stop.ps1` | 開、關模擬器 |

LEO 專案搬家的話,改 `start.ps1` 第 6 行的路徑。
